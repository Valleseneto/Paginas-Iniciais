//document.write serve para escrever no documento ("")
//alert gera um alerta na tela e isso muda de acordo com cada navegador
function mudaFoto (foto){
    document.getElementById("icone").src = foto;
}